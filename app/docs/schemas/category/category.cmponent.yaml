components:
  schemas:
    Category:
      type: object
      required:
        - name
        - icon
        - isPublished
      properties:
        name:
          type: string
          description: The name of the category
        icon:
          type: string
          description: The icon representing the category
        isPublished:
          type: boolean
          description: Whether or not the category is published
